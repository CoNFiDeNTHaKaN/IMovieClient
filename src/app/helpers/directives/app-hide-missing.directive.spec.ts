import { AppHideMissingDirective } from './app-hide-missing.directive';

describe('AppHideMissingDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHideMissingDirective();
    expect(directive).toBeTruthy();
  });
});
